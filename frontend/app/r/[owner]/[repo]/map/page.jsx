"use client";

// Placeholder - real Level 1/2/3 box map + Developer-mode force-graph
// arrives in 2.0 Week 2. This route exists now so the topbar's Map tab
// works and the skeleton is genuinely all 6 routes, not 5 + a promise.

import { COLORS } from "../../../../theme.js";

export default function MapPage() {
  return <p style={{ color: COLORS.textSecondary }}>Map page — coming in Gitography 2.0 Week 2.</p>;
}
